//  需求：1. 定义数组 var arr = [11,22,33,44,55],要求：使用for循环将arr数组的中的元素全部打印到控制台

// 动用一个for循环，让循环中的变量i作为数组元素索引,取值范围到数组长度之间
var arr = [11,22,33,44,55]
for (var i=0; i<arr.length; i++)
{
    // 输出每个元素
    console.log(arr[i]);
}
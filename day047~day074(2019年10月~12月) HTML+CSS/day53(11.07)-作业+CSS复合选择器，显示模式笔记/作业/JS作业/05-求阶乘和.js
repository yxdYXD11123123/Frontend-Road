// 5.求1!+2!+3!+...+20!的和

// 步骤：
// 声明一个变量，作为和
let sum = 0;
// 循环1-20所有数
for (let i = 1; i <= 20; i++) {
    // 声明一个变量，作为阶乘
    let jie = 1
    // 用循环求出每个阶乘
    for (let j = 1; j <= i; j++) {
        jie = jie * j
    }
    // 将每个阶乘加入总和
    sum += jie;
}
// 输出阶乘和
console.log(jie);
